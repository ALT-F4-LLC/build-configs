name: flake

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - uses: DeterminateSystems/nix-installer-action@v4

    - uses: actions/checkout@v3

    - run: nix build --json
